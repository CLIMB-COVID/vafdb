name: vafdb
channels:
  - defaults
  - conda-forge
  - anaconda
dependencies:
  # sudo apt-get install postgresql postgresql-contrib
  # sudo apt-get install rabbitmq-server
  - gunicorn
  - pybind11
  - pip
  - pip:
    - psycopg2
    - django
    - djangorestframework
    - requests
    - pysam
    - numpy
    - pandas
    - git+https://github.com/tombch/basecount@209628936540e63b6267159732adfb25e1e9055d
    - celery
